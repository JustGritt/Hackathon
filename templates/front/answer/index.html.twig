<!DOCTYPE html>

<title>Answer index</title>

{% block body %}
    <h1>Answer index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Content</th>
                <th>Is_response</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for answer in answers %}
            <tr>
                <td>{{ answer.id }}</td>
                <td>{{ answer.content }}</td>
                <td>{{ answer.isResponse() ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('front_app_answer_show', {'id': answer.id}) }}">show</a>
                    <a href="{{ path('front_app_answer_edit', {'id': answer.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('front_app_answer_new') }}">Create new</a>
{% endblock %}
