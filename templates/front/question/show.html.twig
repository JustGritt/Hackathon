<!DOCTYPE html>

<title>Question</title>

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ question.content }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ question.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('front_app_question_index') }}">back to list</a>

    <a href="{{ path('front_app_question_edit', {'id': question.id}) }}">edit</a>

    {{ include('front/question/_delete_form.html.twig') }}
{% endblock %}
