<form method="post" action="{{ path('front_app_hk_video_delete', {'id': hk_video.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hk_video.id) }}">
    <button class="btn">Delete</button>
</form>
