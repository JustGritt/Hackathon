{% extends 'front_base.html.twig' %}
{% block title %}CommentaireVideo!{% endblock %}

{% block body %}
    <h1>CommentaireVideo index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Commentaire</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaire_video in commentaire_videos %}
            <tr>
                <td>{{ commentaire_video.id }}</td>
                <td>{{ commentaire_video.commentaire }}</td>
                <td>
                    <a href="{{ path('front_app_commentaire_video_show', {'id': commentaire_video.id}) }}">show</a>
                    <a href="{{ path('front_app_commentaire_video_edit', {'id': commentaire_video.id}) }}">edit</a>
                    
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('front_app_commentaire_video_new') }}">Create new</a>
{% endblock %}
