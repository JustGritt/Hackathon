<form method="post" action="{{ path('front_app_commentaire_video_delete', {'id': commentaire_video.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire_video.id) }}">
    <button class="btn">Delete</button>
</form>
