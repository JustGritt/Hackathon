{% for message in app.flashes('success') %}
   <div id="notyf-message" class="success invisible">{{ message }}</div>
{% endfor %}
{% for message in app.flashes('error') %}
   <div id="notyf-message" class="error invisible">{{ message }}</div>
{% endfor %}
{% for message in app.flashes('info') %}
   <div id="notyf-message" class="info invisible">{{ message }}</div>
{% endfor %}
{% for message in app.flashes('warning') %}
   <div id="notyf-message" class="warning invisible">{{ message }}</div>
{% endfor %}